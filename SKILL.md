---
name: word-style-standardization
aliases:
  - word-style-normalize
  - 标准样式Word规整
  - Word样式规整
  - 投标文档样式规整
  - normalize-docx-style
summary: 按标准 Word 模板对 .docx 文件进行原位样式规整，尽量保留图片、表格、页眉页脚、对象锚点、媒体引用、分页关系和嵌入对象，并输出校验报告。
description: Use this skill when the user asks to standardize, normalize, clean, or apply the provided standard Word style template to a .docx document, especially Chinese tender/solution/proposal documents. This skill should modify styles in-place rather than rebuilding the document, preserve embedded objects and media, avoid direct Normal/正文 style usage, apply heading/body/table/image/list styles from the standard template, and produce a verification report comparing source and output counts.
---

# Word 标准样式规整 Skill

## 适用场景

当用户提出以下需求时，应触发本技能：

- 将某个 Word 文件按“标准样式Word文档”进行样式规整。
- 对投标文件、技术方案、实施方案、咨询报告、汇报材料进行 Word 样式统一。
- 要求保留原文件图片、表格、页眉页脚、对象锚点、分页、嵌入媒体和关系引用。
- 指出“之前图片丢了”“不要重建正文”“按模板样式套用”等问题。
- 要求检查新旧文档的图片数量、媒体数量、段落数量、表格数量、默认正文样式数量。

## 核心原则

本技能的核心原则是：**不重建文档正文，而是在原 Word 文件上进行原位样式规整**。

不要采用“读取正文文本后重新生成 docx”的方式处理正式文件。该方式容易造成图片、页眉页脚、表格、对象锚点、媒体引用、分节符、书签、批注、脚注、尾注、交叉引用、目录域、嵌入对象等内容丢失。正确做法是在源 `.docx` 压缩包结构中保留原有文件关系，仅对 WordprocessingML 中的段落样式、表格段落样式、图片段落样式和必要编号属性进行有限修改。

## 标准样式映射

默认样式映射如下：

| 内容类型 | 默认处理规则 | 模板 styleId |
| --- | --- | --- |
| 一级标题 | 套用模板中的“标题 1,标准标题 1”，标题不手工编号 | `10` |
| 二级标题 | 套用模板中的“标题 2,标准标题 2”，标题不手工编号 | `20` |
| 三级标题 | 套用模板中的“标题 3,标准标题 3”，标题不手工编号 | `30` |
| 四级标题 | 套用模板中的“标题 4,标准标题 4”，标题不手工编号 | `40` |
| 五级标题 | 套用模板中的“标题 5,标准标题 5”，标题不手工编号 | `50` |
| 六级标题 | 套用模板中的“标题 6,标准标题 6”，标题不手工编号 | `6` |
| 七级标题 | 套用模板中的“标题 7,标准标题 7”，标题不手工编号 | `7` |
| 八级标题 | 套用模板中的“标题 8,标准标题 8”，标题不手工编号 | `8` |
| 九级标题 | 套用模板中的“标题 9,标准标题 9”，标题不手工编号 | `9` |
| 正文段落 | 套用“P_正文段落1.5倍段前段后0.5行” | `P1505` |
| 粗体正文 | 套用“P_普通列表样式_粗体字” | `P` |
| 表格本体 | 套用“表格标准样式” | 从模板按样式名解析 |
| 表格内容 | 套用“P_普通图表标准格式_表格” | `P5` |
| 图片段落 | 套用“P_普通图表标准格式_图片” | `P6` |
| 普通列表一级 | 套用“P_普通列表样式2_箭头” | `P2` |
| 普通列表二级 | 套用“P_普通列表样式3_缩进打钩符” | `P3` |
| 普通列表三级 | 套用“P_普通列表样式4_缩进四角星” | `P4` |

## 处理流程

### 文件准备

将待规整的源文件放在工作目录，例如：

```bash
input/source.docx
```

模板文件可使用本技能 `assets/标准样式Word文档.docx`，也可由用户另行提供。若用户提供了新版本标准模板，应优先使用用户本次提供的模板。

### 推荐执行命令

```bash
python scripts/normalize_word_style.py \
  --input input/source.docx \
  --template assets/标准样式Word文档.docx
```

默认输出到 `output/<源文件名>_标准样式规整.docx`，校验报告输出到 `output/<源文件名>_样式规整校验报告.md`。如需自定义路径，可显式传入 `--output` 和 `--report`。

### 处理动作

脚本应执行以下动作：

- 复制源 `.docx` 为输出文件，不重建正文内容。
- 将模板中的 `word/styles.xml`、`word/numbering.xml`、`word/theme/theme1.xml` 尽量复制到输出文件，确保模板样式可用。
- 复制模板样式前，应先读取源文档 `word/styles.xml`，记录源 styleId 对应的标题、正文、表格、图片、重点文本等语义；真实投标文档常用非模板 styleId，例如 `43`、`54`、`61`，不能只按硬编码 styleId 判断。
- 遍历 `word/document.xml`、页眉、页脚、脚注、尾注等正文相关 XML 文件。
- 根据段落位置、文本特征、现有样式、编号层级、是否位于表格、是否包含图片或绘图对象，判断应套用的目标样式。
- 对标题段落套用对应标题样式。标题层级只能根据源文档已有 heading 1-9 样式或继承自 heading 1-9 的样式判断，不得仅凭文本编号、中文编号或阿拉伯数字编号推断标题。
- 对表格本体套用模板中的“表格标准样式”。应从模板 `word/styles.xml` 按样式名解析真实 table styleId，不得硬编码某个 styleId。
- 表格本体只改写 `w:tblPr/w:tblStyle` 和必要的 `w:tblLook`，不得重建表格，不得改动行列结构、合并单元格、表格内图片或嵌入对象。
- 对表格单元格清理会覆盖表格样式的直接边框和底纹，即 `w:tcPr/w:tcBorders`、`w:tcPr/w:shd`；不得删除 `gridSpan`、`vMerge`、`tcW`、`vAlign`、`textDirection` 等结构和排版属性。
- 对表格单元格中的普通段落套用表格样式。
- 对标题段落清理段落和文字直接格式，使标题尽量完全服从模板标题样式。
- 对正文、列表、表格段落清理字体、字号、颜色、段落间距和缩进等直接格式；保留加粗、斜体、下划线、高亮等可能承载语义的文字格式。
- 对包含 `w:drawing`、`w:pict`、`a:blip` 等图片或对象的段落套用图片样式。
- 对普通正文套用正文段落样式。
- 对正文中的中文编号、数字编号、括号编号保留为手工编号文本，不套用 `P1`、`P2123`、`P3123` 等模板编号样式。
- 除标题外，正文段落若存在 Word 自动编号，应先根据源文档 `word/numbering.xml` 的 `numId`、`ilvl`、`numFmt`、`lvlText` 计算可见编号文本，插入段首变成手工编号，再移除 `w:numPr`，避免正文继续依赖 Word 自动编号。
- 只有正文文本本身包含明确项目符号（例如 `•`、`●`、`→`、`✓`、`- `），或源文档自动编号定义的 `numFmt` 明确为 `bullet` 时，才套用箭头、缩进打钩符、缩进四角星样式。
- 不得把 decimal/chinese/括号编号等自动编号套成 `P2/P3/P4`；这类编号应先转为 `1)`、`一、` 等手工编号文本，再套正文样式，避免出现“箭头样式 + 1)”的重复编号。

### 必须校验

生成文件前后必须做校验，并输出报告。校验项至少包括：

| 检查项 | 目的 |
| --- | --- |
| 原文件图片引用数量与新文件图片引用数量 | 确认图片没有丢失 |
| 原文件媒体文件数量与新文件媒体文件数量 | 确认内嵌图片资源保留 |
| 段落数量 | 确认正文没有被重建或遗漏 |
| 表格数量 | 确认表格没有丢失 |
| 表格标准样式套用情况 | 确认表格本体使用模板“表格标准样式” |
| 直接格式清理情况 | 确认表格边框/底纹、标题直接格式、正文基础直接格式已清理 |
| 默认“正文/Normal”样式数量 | 确认所有内容均套用了模板样式 |
| 列表样式套用情况 | 确认箭头、缩进打钩符、缩进四角星按层级套用 |
| 标题样式套用情况 | 确认标题使用模板自带样式 |

## 质量红线

- 不得通过复制纯文本重建 Word 文件。
- 不得删除 `word/media/` 下的任何资源。
- 不得删除 `_rels`、`word/_rels`、页眉页脚、脚注尾注、批注、图表、嵌入对象等关系文件。
- 不得将所有段落简单套成同一种正文样式。
- 不得手工给标题写编号，标题编号应由模板样式控制。
- 若脚本发现处理后媒体文件数量减少，必须停止并提示失败，不得交付该文件。
- 若默认正文样式数量仍明显偏高，应在报告中说明残留原因，例如空段落、目录域、特殊对象段落等。

## 交付口径

完成后向用户交付：

- 规整后的 `.docx` 文件。
- 样式规整校验报告 `.md` 或 `.txt`。
- 简要说明：采用原位处理方式，保留源文档对象关系；已核对图片、媒体、段落、表格、默认正文样式、列表样式、标题样式。

## 适用限制

- 本技能主要面向 `.docx` 文件；旧版 `.doc` 文件应先转换为 `.docx` 再处理。
- 极复杂文档包含宏、受保护内容、嵌入 Visio/Excel/OLE 对象时，脚本通常仍保留包内对象，但样式套用可能无法覆盖对象内部内容。
- 对扫描版 PDF 或图片文件不适用，应先转换为 Word 或另行处理。
